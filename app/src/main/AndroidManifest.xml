<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zl.dagger2example">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lyfdi2.SubComponent.SonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SecondActivity" />
        <activity android:name=".lyfdi2.Lazy.LazydiActivity" />
        <activity android:name=".lyfdi2.Scope.ScopediActivity2" />
        <activity android:name=".lyfdi2.dependencies.ManActivity" />
        <activity android:name=".lyfdi2.SubComponent.FatherActivity" />

    </application>

</manifest>
